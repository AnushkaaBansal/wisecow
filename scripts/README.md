# Scripts Usage

## System Health Check
```bash
# Make the script executable
chmod +x system_health.sh

# Run the script
./system_health.sh

# Make the script executable
chmod +x backup.sh

# Run the script
./backup.sh /path/to/source user@remote:/backup/path
